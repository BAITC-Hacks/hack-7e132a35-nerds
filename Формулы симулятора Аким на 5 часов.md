# Формулы симулятора «Аким на 5 часов»

**Как читать модель.** Симулятор пересчитывает десять показателей качества жизни в каждом районе после выбранных мероприятий. Затем он считает районные индексы и общий балл города. Формулы ниже используют показатели и правила из датасета; числовые эффекты мероприятий — сценарные параметры учебной модели, а не гарантированный прогноз реального результата.

## Коротко: порядок расчёта

**План решений → эффекты с учётом лага → новые показатели районов → индекс каждого района → городской балл.**

Сначала проверяются правила плана: ровно пять разных мероприятий, стоимость не выше 100 условных единиц, не более двух мероприятий одного направления, указаны необходимые районы и не нарушены несовместимости. Если план не проходит проверку, балл не рассчитывается.

## Обозначения

| Обозначение | Значение |
|---|---|
| `d` | Район: Есиль, Алматы, Сарыарка, Байконур или Нура |
| `k` | Показатель: `T1`, `T2`, `E1`, `E2`, `S1`, `S2`, `B1`, `B2`, `C1` или `C2` |
| `m` | Выбранное мероприятие |
| `I[d,k]` | Исходное значение показателя района, от 0 до 100 |
| `E[m,k]` | Полный эффект мероприятия на показатель из каталога; знак минус означает ухудшение |
| `L[m]` | Лаг мероприятия в кварталах |
| `H` | Горизонт симуляции: 8 кварталов |
| `p[d]` | Доля населения района; доли всех районов в сумме дают 1 |
| `w[k]` | Вес показателя; все веса в сумме дают 1 |
| `clamp(x, 0, 100)` | Ограничивает `x` диапазоном от 0 до 100 |

Все районные показатели направлены одинаково: **чем выше значение, тем лучше**. Например, высокий `T1` означает меньше пробок, а высокий `E2` — лучшее качество воздуха.

## 1. Доля эффекта, которая успевает проявиться

На горизонте в восемь кварталов мера с лагом получает такую долю полного эффекта:

$$
r_m = \operatorname{clamp}\!\left(\frac{H-L_m}{H},\,0,\,1\right), \qquad H=8
$$

Для обычных значений из каталога это правило датасета: `(8 − лаг) / 8`. Чем позже начинает работать мера, тем меньшая часть её эффекта попадает в симуляцию.

## 2. Новые значения показателей по районам

$$
I'_{d,k}=\operatorname{clamp}\!\left(
I_{d,k}
+\sum_{m\in M} a_{m,d}\,E_{m,k}\,r_m
+S_{d,k},\;0,\;100
\right)
$$

Здесь:

- `M` — выбранные мероприятия;
- `a[m,d] = 1`, если мероприятие действует в районе `d`, и `0` в противном случае;
- для меры типа **«Район»** эффект применяется только к указанному району;
- для меры типа **«Город»** эффект применяется ко всем пяти районам;
- `S[d,k]` — сумма подходящих бонусов синергии для этого района и показателя;
- бонус синергии добавляется один раз и **не умножается на долю лага**;
- после сложения результат ограничивается диапазоном 0–100.

Отрицательные эффекты сохраняют знак. Например, если мероприятие повышает `B2` на 12, но снижает `T1` на 2, оба изменения учитываются в соответствующих показателях.

### Как применяются синергии

Бонус добавляется, только если выбраны обе меры из пары в каталоге синергий. Для пар районных мер он применяется в районе первой меры пары. Синергии из текущего датасета: `M1 + M2`, `M10 + M12`, `M5 + M6`.

## 3. Индекс качества жизни района

Индекс района — взвешенное среднее десяти показателей после применения мероприятий:

$$
\begin{aligned}
D_d ={}& 0.10\,T1_d + 0.10\,T2_d + 0.09\,E1_d + 0.11\,E2_d \\
      &+ 0.11\,S1_d + 0.11\,S2_d + 0.09\,B1_d + 0.09\,B2_d \\
      &+ 0.10\,C1_d + 0.10\,C2_d
\end{aligned}
$$

В этой формуле обозначения показателей означают **новые значения** `I'[d,k]`. Их веса заданы датасетом:

| Направление | Показатели и веса |
|---|---|
| Транспорт | `T1 × 0.10`, `T2 × 0.10` |
| Экология | `E1 × 0.09`, `E2 × 0.11` |
| Социальная сфера | `S1 × 0.11`, `S2 × 0.11` |
| Безопасность | `B1 × 0.09`, `B2 × 0.09` |
| Сервисы | `C1 × 0.10`, `C2 × 0.10` |
| **Сумма весов** | **1.00** |

Индекс `D[d]` также лежит в диапазоне от 0 до 100.

## 4. Средний индекс города с учётом населения

$$
D_{\mathrm{avg}}=\sum_d p_d\,D_d
$$

В модели районы взвешиваются по доле населения: Есиль — `0.27`, Алматы — `0.24`, Сарыарка — `0.20`, Байконур — `0.13`, Нура — `0.16`. Сумма долей — `1.00`.

## 5. Штраф за критически низкие показатели

$$
N_{\mathrm{crit}}=\#\left\{(d,k): I'_{d,k}<40\right\}
$$

Считается каждая пара **«район × показатель»** ниже 40. Например, если в одном районе два показателя ниже 40, это два критических значения. Ровно 40 критическим не считается.

## 6. Итоговый городской балл

$$
\boxed{\mathrm{Score}=\operatorname{clamp}\!\left(
0.70\,D_{\mathrm{avg}} + 0.30\,\min_d(D_d) - 1.0\,N_{\mathrm{crit}},
\;0,\;100
\right)}
$$

**Что означает формула:** 70% балла отражает средний результат по городу, взвешенный по населению; 30% учитывает самый низкий районный индекс; каждое критическое значение ниже 40 вычитает один балл. Ограничение `clamp` удерживает итог на шкале 0–100.

### Проверка базового результата из датасета

В датасете указаны `D_avg = 56.86`, минимальный районный индекс `49.18` и два критических значения в Нуре (`S1` и `S2`):

$$
0.70\times56.86+0.30\times49.18-1.0\times2
=52.556\approx\mathbf{52.56}
$$

## 7. Проверка допустимости плана

До расчёта результата валидатор проверяет все условия:

1. Выбрано **ровно 5 мероприятий**.
2. Все мероприятия разные; каждое можно выбрать не более одного раза.
3. Суммарная стоимость не превышает **100 условных единиц**.
4. Выбрано не более двух мероприятий одного направления.
5. Для каждой меры типа «Район» задан район; для городской меры район не задаётся.
6. Не выбраны запрещённые сочетания: `M1 + M3`; `M4 + M7` в одном районе; `M5 + M13` в одном районе.

Если нарушено хотя бы одно условие, симулятор сообщает причину, а Score не показывает. Неизрасходованный бюджет не сгорает и не даёт бонуса.

## 8. Точный порядок вычислений

1. Проверить бюджет и правила выбора.
2. Рассчитать реализованную долю эффекта каждого мероприятия с учётом лага.
3. Применить районные и городские эффекты к исходным показателям.
4. Добавить бонусы сработавших синергий.
5. Ограничить каждый новый показатель диапазоном 0–100.
6. Рассчитать районные индексы `D[d]`.
7. Рассчитать `D_avg` по долям населения и посчитать `N_crit`.
8. Рассчитать городской Score.
9. Округлять числа только при отображении; промежуточные значения не округлять.

## 9. Что показывать участнику

- Базовый и новый Score, а также изменение `ΔScore = Score после − Score до`.
- Индекс до и после для каждого района.
- Изменения по каждому показателю и вклад выбранных мероприятий.
- Сработавшие синергии, остаток бюджета и значения ниже 40.
- Краткое объяснение результата, составленное **из уже рассчитанных чисел**. ИИ может объяснять и сравнивать сценарии; вычисления и числовые эффекты остаются ответственностью расчётной модели.

---

**Примечание о статусе модели.** Формула Score и исходные веса взяты из предоставленного датасета. Прогноз отражает правила этой симуляции и не является обещанием фактического эффекта городских инвестиций.
